type Mutation {
    register(input: RegisterInput): AuthResult
    login(input: LoginInput): AuthResult
    logout: BaseResult
}

type AuthResult {
    message: String
    userId: String
    role: Int
}

type BaseResult {
    message: String
}